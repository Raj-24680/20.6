11.
devops1   ✅  ← THIS is your project
 ├── src
 ├── calculator.java
 ├── CalculatorTest.java
Right click:
  devops1   ✅
  Build Path → Add Libraries
  JUnit → Next → JUnit 4 → Finish
  Apply and Close
Before doing this, delete this file:
	module-info.java
Right click CalculatorTest.java → Run As → JUnit Test
package devops1;

public class calculator {

    public int add(int a, int b) {
        return a + b;
    }

    public int sub(int a, int b) {
        return a - b;
    }

    public int mul(int a, int b) {
        return a * b;
    }

    public int div(int a, int b) {
        if (b == 0)
            throw new ArithmeticException("Cannot divide by zero");
        return a / b;
    }

    public static void main(String[] args) {
        calculator c = new calculator();

        System.out.println("Addition: " + c.add(10, 5));
        System.out.println("Subtraction: " + c.sub(10, 5));
        System.out.println("Multiplication: " + c.mul(10, 5));
        System.out.println("Division: " + c.div(10, 5));
    }
}

package devops1;

import static org.junit.Assert.*;
import org.junit.Test;

public class CalculatorTest {

    calculator c = new calculator();

    @Test
    public void testAdd() {
        assertEquals(15, c.add(10, 5));
    }

    @Test
    public void testSub() {
        assertEquals(5, c.sub(10, 5));
    }

    @Test
    public void testMul() {
        assertEquals(50, c.mul(10, 5));
    }

    @Test
    public void testDiv() {
        assertEquals(2, c.div(10, 5));
    }

    @Test(expected = ArithmeticException.class)
    public void testDivByZero() {
        c.div(10, 0);
    }
}
12.
>>mvn archetype:generate
test code 
>>mvn test
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

13.
. push the 12th file on GitHub
. go to Jenkins slect free style job
.select git in source
.paste url change in main
.add build steps :-invoke top level maven target
.select default or Maven , in goals:- clean install or clean test
.post build action selectpublish junit test report:-target/surefire-reports/*.xml
.build apply

14.
.do same as 12 
.before command >mvn clean 
.open pomfile in notebook or visual studio
.search <build> just after build paste the commandas with same indentation
          <plugins>
        <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>0.8.11</version>
            <executions>
                <execution>
                    <goals>
                        <goal>prepare-agent</goal>
                    </goals>
                </execution>
                <execution>
                    <id>report</id>
                    <phase>test</phase>
                    <goals>
                        <goal>report</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
    </plugins>
.open target,site,jacoco,index.html 

15
docker --version
docker ps
docker ps -a
docker images
docker run hello-world
docker ps -a
docker pull nginx
docker run -d -p 8088:80 nginx
docker rmi nginx
docker logs
docker pause
docker unpause
docker inspect
docker stop  dd34ccf6dda20333544be6f5521e95c7b30a532bcf256714d
docker rm dd34ccf6dda20333544be6f5521e95c7b30a532bcf256714d
16
.create a folder 
.write java file and Dockerfile in it
FROM eclipse-temurin:17-jdk
WORKDIR /app
COPY App.java .
RUN javac App.java
CMD ["java", "App"]
.open cmd
>>docker build -t java-hello .
>>docker run java-hello

17
open cmd
>>docker run -d nginx
>>docker run -d -p 8084:80 nginx
>>docker volume create myvolume
>>docker volume ls
>>docker run -it -v myvolume:/data ubuntu bash
enter-
	#>>cd /data
	#>>echo " volume test" > test.txt
	#>>exit
>>docker run -it -v myvolume:/data ubuntu bash
 enter-
	#>>cat /data/test.txt
	#>>exit
>>docker run -d -p 8087:80 -v myvolume:/usr/share/nginx/html nginx
>>docker run -it -v myvolume:/data ubuntu bash
 enter-
	#>>echo "<h1>persistent web page</h1>" > /data/index.html
	#>>exit
>>docker ps
>>docker volume ls
>>docker volume inspect myvolume

18
.do same as done in 16 till file creation
.open cmd
>>docker build -t java-hello .
>>docker tag java-hello nitinaman/java-hello:latest
>>docker push nitinaman/java-hello:latest
>>docker pull nitinaman/java-hello:latest

19
.do same as 16 with addition of one file Jenkinfiles
.add the code
pipeline {
    agent any

    stages {

        stage('Build Docker Image') {
            steps {
                bat 'docker build -t java-calc-app .'
            }
        }

        stage('Run Container') {
            steps {
                bat 'docker run  java-calc-app'
            }
        }

    }
}
.push it to GitHub
.open Jenkins select pipeline
.select git in SCM
.paste the repository url
.save apply

20)
https://github.com/Raj-24680/20.4

>>docker system prune -a
>>git init
git add .
git commit -m "first commit"
git remote add origin2 https://github.com/Raj-24680/jklo.git
git push -u origin2 main

credential manager
